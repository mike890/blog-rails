<div class="container-fluid">
  <div class="row">
    <div class="col-md-12">
      <div class="jumbotron">
        <fieldset>
            <h1 class="text-center"><%= "#{@article.title}"%></h1>
			<%= render 'form', comment: @comment %>
		</fieldset>
	  </div>
    </div>
   </div>
</div>

